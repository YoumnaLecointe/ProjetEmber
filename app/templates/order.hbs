{{page-title "order"}}
{{#each @model.orders as |order|}}
  <li>
    <LinkTo @route="order" @model={{order.id}}>
      {{order.dateCreation}} <span>{{order.count}}</span>
    </LinkTo>
  </li>
{{/each}}
</ul>